{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "rm -rf ./server/dist && npm run build --prefix client && cp -r ./client/dist ./server",
    "test": "concurrently \"npm run test --prefix PlaywrightTests\" \"npm run dev --prefix client\" \"npm run start:test --prefix server\" ",
    "start": "NODE_ENV=production npm start --prefix server"
  },
  "dependencies": {
    "concurrently": "^10.0.5"
  }
}
